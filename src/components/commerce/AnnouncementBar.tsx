import { Announcement } from "@/components/kibo-ui/announcement";

const AnnouncementBar = () => (
  <div className="w-full max-w-md">
    <Announcement>New feature available</Announcement>
  </div>
);

export default AnnouncementBar;
